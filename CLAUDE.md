# Working agreements

- After every change (no matter how small), commit it and push to `origin/main` on GitHub
  before considering the task done. Don't batch multiple unrelated changes into one commit —
  commit and push per logical change.
