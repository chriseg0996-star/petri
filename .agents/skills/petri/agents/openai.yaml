interface:
  display_name: "Petri"
  short_description: "Repo-specific patterns and workflows for petri"
  default_prompt: "Use the petri repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true